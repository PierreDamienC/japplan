/// <reference types="vite/client" />

// Injecté par vite.config.ts depuis la `version` de package.json — source
// canonique unique, aussi lue par android/app/build.gradle pour versionName.
declare const __APP_VERSION__: string
